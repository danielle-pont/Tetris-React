import React from 'react'

// two prop[ertie]s will be passed into the display
const Display = ({ gameOver, text }) => <div>{text}</div> // return is implicit, so no () needed to encase the div

export default Display
